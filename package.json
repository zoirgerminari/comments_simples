{
  "name": "comments-simples",
  "version": "2.0.0",
  "description": "Sistema de comentários com Neon Database e Netlify",
  "type": "module",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build step required'",
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "keywords": ["comentarios", "netlify", "neon", "database", "postgresql"],
  "author": "zoirgerminari",
  "license": "MIT"
}
